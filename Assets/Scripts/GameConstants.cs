/// <summary>
/// Gom tất cả magic numbers vào một chỗ duy nhất.
/// Khi cần thay đổi giá trị, chỉ sửa ở đây.
/// </summary>
public static class GameConstants
{
    public const int BoardSize = 8;
    public const int BlockPreviewSize = 5;
    public const float InputYOffset = 2.0f;
    public const float HoverAlpha = 0.5f;
    public const float CellPositionOffset = 0.5f;
}
